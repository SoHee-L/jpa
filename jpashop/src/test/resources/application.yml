spring:
#스프링 부트는 기본적으로 설정이 없으면 다 메모리 모드로 돌려버려서 밑에 소스들은 필요없음.
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#
#    properties:
#      hibernate:
##        show_sql: true (system.out~)
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace